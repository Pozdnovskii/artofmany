# pnpm skips dependency build scripts unless allow-listed here. These are
# native packages whose binaries the build needs.
allowBuilds:
  esbuild: true
  # sharp is only a transitive (optional) dep of Astro; we never invoke it
  # (imageService: passthrough), so skip building its native binary.
  sharp: false
  workerd: true
